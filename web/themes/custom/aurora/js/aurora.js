/**
 * @file
 * Aurora behaviors. IIFE
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.aurora = {
    attach: function (context, settings) {

      console.log('It works! Or does it?');

    }
  };

} (Drupal));
